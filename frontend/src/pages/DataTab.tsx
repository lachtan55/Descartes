/**
 * DataTab — placeholder.
 * Price data management (yFinance fetch, stored datasets) has moved to the
 * DATABASE page → "DATABASE" tab.
 */
export default function DataTab() {
  return (
    <div style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: '60vh',
      fontFamily: 'var(--font-mono)',
      fontSize: 11,
      color: 'var(--text-muted)',
      letterSpacing: '2px',
    }}>
      PRICE DATA IS MANAGED IN THE DATABASE PAGE.
    </div>
  );
}
